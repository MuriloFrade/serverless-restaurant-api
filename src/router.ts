
import { Express } from 'express'

import { RestaurantCtrl } from './controllers/restaurant-ctrl'

export class Router {

  static createRoutes (app: Express) {
    // const restaurantCtrl = new RestaurantCtrl()
    // app.post('/restaurants', restaurantCtrl.create)
  }
}
